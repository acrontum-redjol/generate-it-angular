{% for endpoint in endpoints -%}
import { {{ ucFirst(endpoint) }}Service } from './{{ ucFirst(endpoint) }}Service';
{% endfor %}

export {
  {% for endpoint in endpoints -%}
  {{ ucFirst(endpoint) }}Service,
  {% endfor %}
}

